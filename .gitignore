# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# ── 虚拟环境 ──
.venv/
venv/
env/

# ── 测试缓存 ──
.pytest_cache/
*.pytest_cache*

# ── 项目数据文件（隐私） ──
portfolio.json
portfolio_migrated_candidate.json
portfolio_migrated_candidate.backup-*.json
portfolio_migrated_candidate_old.json
position-information-*.xlsx
portfolio_config.json
usmart_holdings.*
short_watchlist.json
portfolio_history.json
account_config.json
credentials.json
secrets.json

# ── 筛选结果 ──
美股筛选结果_*.csv
美股筛选结果_*.xlsx

# ── 每日晨报 ──
每日晨报/

# ── 分析输出 ──
分析脚本/
数据文件/

# ── 个股研究报告 ──
个股研究/

# ── 交易记录 ──
交易记录/

# ── IDE ──
.agents/
.vscode/
.idea/
*.swp
*.swo
*~

# ── 系统 ──
.DS_Store
Thumbs.db

# ── 环境变量 ──
.env
.env.*
!.env.example

# ── 日志 ──
*.log
logs/

# ── 运行锁文件 ──
.run_locks/

# ── 运行时生成的数据文件 ──
northstar/data/strategy_feedback.json
northstar/data/system_state.json
northstar/data/recommendation_review_snapshots.json
northstar/data/recommendation_review*.json

# ── Streamlit 配置缓存 ──
.streamlit/
.streamlit/secrets.toml
.streamlit/config.toml

# ── 类型检查/代码检查缓存 ──
.mypy_cache/
.ruff_cache/
.aider*

# ── 运行缓存 ──
.cache/
northstar/runtime/
northstar.lock
northstar.browser_opened
northstar_crash.log

# ── 运行时用户数据 ──
northstar/data/recommendations.json
logs/codex_*.txt

# ── 临时恢复文件 ──
restored_ui.py
polaris.py

# ── 测试临时文件 ──
tests/__pycache__/
tests/.pytest_cache/

# ── 运行报告（每日生成，不提交） ──
reports/*.json
reports/*.md
reports/*.patch
reports/*.txt
reports/daily_decision/

# ── 审计文档（一次性修复记录，不提交） ──
docs/audit/

# ── 调试脚本（本地专用，不提交） ──
debug_asset_holding.py
debug_usmart_full_test.py
debug_usmart_login.py

# ── 备份文件 ──
*.backup-*.json
*.backup-*
*_backup/
backup/

# ── 项目外的同步备份目录 ──
../北极星同步备份/

# ── 缓存文件 ──
.backtest_cache.json
eval_data.json
execution_queue.json
historical_run_log.txt
instruments.json
state_snapshot.json