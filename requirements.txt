# === 美股研究与监控系统 ===
# 安装：pip install -r requirements.txt
#
# 分类说明：
#   [核心]   — 主系统运行必需（看盘、分析、持仓只读）
#   [工具]   — 独立工具脚本所需
#   [可选]   — 非必需，按需安装

# ── [核心] 行情数据 ──
yfinance>=0.1.70

# ── [核心] 数据处理 ──
pandas>=1.5.0
numpy>=1.24.0
streamlit>=1.35.0

# ── [工具] 抓取机器人（需图形界面） ──
pyautogui>=0.9.53
Pillow>=9.0.0

# ── [可选] 测试 ──
# 当前测试使用 unittest（标准库），无需额外安装
# pytest>=7.0.0
